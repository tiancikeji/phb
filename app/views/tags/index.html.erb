<div class="widget_container">
  <div class="well">
    <div class="navbar navbar-static navbar_as_heading">
      <div class="navbar-inner">
        <div class="container" style="width: auto;">
          <a class="brand">Comments</a>
        </div>
      </div>
    </div>

    <div class="row-fluid">
      <% @tags.each do |tag| %>
        <%= link_to tag.name, tag_path(tag.name) %>
      <% end %>

    </div>

  </div>
</div>
